[project]
name = "giwaxs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sophie", email = "sophie.tucker707@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.4.6",
    "scipy>=1.17.1",
]

[project.scripts]
giwaxs = "giwaxs:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
